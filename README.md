# dbms_training
git init
git add *.*
git commit -m "message or title"
git remote add origin https://github.com/VaibhavRathod01-IARE/dbms_training.git
git checkout -b master
git push origin master

# if i want to update the repository
git status
git add *.*
git commit -m "message or title"
git push origin master
